#Варіант 23
#Дано три точки на площині.Визначити, яка з них ближче до початку координат.

import math
min=float
#Вводим значення координат точок.
x1=float(input("Введіть x1:"))
x2=float(input("Введіть x2:"))  
x3=float(input("Введіть x3:"))
y1=float(input("Введіть y1:"))
y2=float(input("Введіть y2:"))
y3=float(input("Введіть y3:"))
#Знаходим довжину від кожної точки до початку координат.
OA= math.sqrt((x1 * x1) + (y1 * y1)) 
OB= math.sqrt((x2 * x2) + (y2 * y2))
OC= math.sqrt((x3 * x3) +( y3 * y3))

if (OA < OB and OA < OC):
		min = OA
		print("1 Точка,яка задана координатами х1,y1 , знаходиться найближче до початку координат.")
	
else :
		if (OB < OC) :
			min = OB
			print("2 Точка,яка задана координатами х2,y2 , знаходиться найближче до початку координат.")
		
		else :
		     min = OC
		     print("3 Точка,яка задана координатами х3,y3 , знаходиться найближче до початку координат.")
#Знаходим мінімальну довжину від точок до початку координат.
